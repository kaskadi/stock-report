service:
  name: kaskadi-stock-report-lambda

package:
  individually: true
  exclude:
    - '**/**'

provider:
  name: aws
  runtime: nodejs12.x
  stackName: ${self:service.name}-stack
  stage: ${opt:stage, 'prod'}
  region: eu-central-1
  deploymentBucket:
    name: kaskadi-serverless-deployment-bucket
  tags:
    app: kaskadi
    service: ${self:service.name}
    logical-unit: stocks
    type: schedule

layers:
  KaskadiStockReportLayer:
    path: layer
    name: ${self:service.name}-layer
    description: Layer for ${self:service.name}
    compatibleRuntimes:
      - nodejs10.x
      - nodejs12.x
    licenseInfo: MIT
    package:
      include:
        - '**/**'

functions:
  KaskadiStockReport:
    handler: kaskadi-stock-report-lambda.handler
    name: ${self:service.name}
    package:
      include:
        - 'kaskadi-stock-report-lambda.js'
        - 'helpers/**'
    layers:
      - { Ref: KaskadiStockReportLayerLambdaLayer }
    environment:
      ES_ID: ${env:ES_ID}
      ES_SECRET: ${env:ES_SECRET}
      ES_ENDPOINT: ${env:ES_ENDPOINT}
    events:
      - schedule:
          rate: cron(0 7 ? * MON *) # runs every monday at 7AM
          enabled: true
